server:
  port: 8092
spring:
  application:
    name: server-consumer
eureka:
  client:
    service-url:
      defaultZone: http://localhost:10087/eureka/
feign:
  hystrix:
    enabled: true #开启feign的熔断功能
